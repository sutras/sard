/*
### 内置图标
*/

import { Icon } from 'sard'

export default function () {
  return (
    <>
      <div style={{ fontSize: 20 }}>
        <Icon prefix="si" name="fail" />
        <Icon prefix="si" name="success" />
        <Icon prefix="si" name="close" />
        <Icon prefix="si" name="question" />
        <Icon prefix="si" name="info" />
        <Icon prefix="si" name="minus" />
        <Icon prefix="si" name="plus" />
        <Icon prefix="si" name="up" />
        <Icon prefix="si" name="down" />
        <Icon prefix="si" name="left" />
        <Icon prefix="si" name="right" />
        <Icon prefix="si" name="search" />
        <Icon prefix="si" name="person" />
      </div>
    </>
  )
}
