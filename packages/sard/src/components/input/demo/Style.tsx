/*
### 自定义样式
*/

import { Input } from 'sard'

export default function () {
  return (
    <Input
      style={{
        color: 'red',
        borderColor: 'red',
      }}
      placeholder="自定义样式"
    />
  )
}
