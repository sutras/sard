import { Tag } from 'sard-taro'

export default () => {
  return (
    <Tag
      style={{
        background: 'rgba(var(--sar-pink-rgb), 0.1)',
        color: 'var(--sar-pink)',
      }}
    >
      标签
    </Tag>
  )
}
