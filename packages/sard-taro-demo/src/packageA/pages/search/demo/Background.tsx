import { Search } from 'sard-taro'

export default () => {
  return (
    <Search
      placeholder="请输入关键词"
      background="var(--sar-danger)"
      inputBackground="#fff"
      shape="round"
    />
  )
}
